# 2x2
a virtual 2x2 cube, complete with timer, scrambler, editor, solver, optimum first side finder, etc
- solver doesn't work with cube rotations yet
- it's also very slow sometimes
- optimum side solver doesn't have rotation support, so you'll have to modify the solution yourself
